// 방 정원·닉네임 길이 — 팀 논의로 계속 바뀔 수 있는 값이라 한 곳에서만 관리한다.
// ★ 서버도 같은 검사를 한다 (방 목록 기능) — apps/backend/src/room.ts 의 MIN_PLAYERS·
// MAX_PLAYERS 와 반드시 같은 값이어야 한다. 한쪽만 고치면 "목록에선 들어갈 수 있어
// 보이는데 서버가 거절"하는, 화면에 원인이 안 드러나는 상태가 된다.
// (공유하려고 shared-types 에 뒀다가 되돌렸다 — 그 패키지는 타입 전용이라 런타임
//  값을 넣으면 백엔드 빌드 산출물이 실행 중 죽는다. 그 파일 주석 참고.)
// 봇 포함. 게임이 시작되는 최소 인원 — 서버가 ready 처리에서 검사한다.
// 화면에서는 쓰지 않는다(게임시작 버튼을 없애 판단 지점이 서버 하나로 모였다).
// 서버 값과 어긋나면 안 되므로 짝으로 남겨둔다.
export const MIN_PLAYERS = 4;
export const MAX_PLAYERS = 8; // 봇 포함. 방 정원 고정값 (방장이 개별 설정하지 않음)

// 닉네임 최대 글자수. 위와 같은 규칙으로 서버 room.ts 의 NAME_MAX_LENGTH 와 짝이다 —
// 여기만 늘리면 입력창은 받아주는데 서버 join 이 거절해서, 시작 버튼을 눌러도 에러
// 배너만 뜨고 방에 못 들어가는 상태가 된다.
// 이 값이 곧 결과 화면에 뜨는 이름의 상한이다 — 설문화면 우측 패널(260px)이 이 길이를
// 최악의 경우로 잡고 있어서, 늘리면 그쪽 줄바꿈부터 확인해야 한다.
export const NAME_MAX_LENGTH = 6;
